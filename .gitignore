.DS_Store

# Geuploade foto's (fase M4) -- lokaal/op het volume, niet in git
uploads/*
!uploads/.gitkeep
data/

# Composer (fase M5) -- vendor/ wordt bij elke Docker-build opnieuw
# geïnstalleerd (zie Dockerfile), hoort dus niet in git.
vendor/
composer.lock
